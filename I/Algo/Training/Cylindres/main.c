#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#include "robot.h"

int main(int argc, const char** argv) {
    deplacer(1, 3);
    deplacer(1, 2);
    deplacer(3, 2);
    deplacer(1, 3);
    deplacer(2, 1);
    deplacer(2, 3);
    deplacer(1, 3);


    
    return EXIT_SUCCESS;
}