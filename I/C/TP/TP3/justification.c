#include <stdlib.h>
#include <stdio.h>
#include <string.h>

#define REALLOC_STEP 10

char* get_text() {
    char* text;
    int i, size;

    size = REALLOC_STEP;
    text = NULL;
}

int main(int argc, const char** argv) {
    char* text;

    return EXIT_SUCCESS;
}