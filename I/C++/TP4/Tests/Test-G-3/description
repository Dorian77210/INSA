Ce test vérifie le comportement de  l’application dans le cas où le fichier contient 
10 documents et que l'option -g est utilisée.
avec hit(page10)>=hit(page9)>=hit(page8)>=hit(page7)>=hit(page6)>=hit(page5)=>hit(page4)>=hit(page3)>=hit(page2)>=hit(page1)