Ce test vérifie le comportement de  l’application dans le cas où le fichier contient 
13 destinations qui ne sont ni de type image ni css ni javascript dont 4 refferers de type image "png","jpg","gif" et "jpeg" et que l'option -e est utilisée.
hit(page13)>=hit(page12)>=hit(page11)>=hit(page10)>=hit(page9)>=hit(page8)=>hit(page7)>=hit(page6)>=hit(page5)=>hit(page4)>=hit(page3)>=hit(page2)>=hit(page1)